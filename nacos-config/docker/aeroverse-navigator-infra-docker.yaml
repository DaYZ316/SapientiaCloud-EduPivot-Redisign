spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/aeroverse_navigator
    username: aeroverse
    password: ${POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: redis
      port: 6379
      password: ${REDIS_PASSWORD}
      database: 0
