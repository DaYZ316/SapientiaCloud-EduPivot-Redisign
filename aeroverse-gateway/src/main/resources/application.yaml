spring:
  application:
    name: aeroverse-gateway
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_SERVER_ADDR:127.0.0.1:8848}
        username: ${NACOS_USERNAME:nacos}
        password: ${NACOS_PASSWORD:nacos}
  config:
    import:
      - optional:nacos:aeroverse-gateway-common.yaml?group=AEROVERSE_NAVIGATOR&refreshEnabled=true

---
spring:
  config:
    activate:
      on-profile: local
    import:
      - optional:nacos:aeroverse-gateway-routes-local.yaml?group=AEROVERSE_LOCAL&refreshEnabled=true

---
spring:
  config:
    activate:
      on-profile: docker
    import:
      - optional:nacos:aeroverse-gateway-routes-docker.yaml?group=AEROVERSE_DOCKER&refreshEnabled=true
